export default function AppLoading() {
  return (
    <div className="flex items-center justify-center py-24">
      <span className="w-8 h-8 border-2 border-[var(--brand-border)] border-t-[var(--brand-navy)] rounded-full animate-spin" />
    </div>
  );
}
